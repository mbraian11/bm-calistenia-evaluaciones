import FormularioEvaluacion from '@/components/FormularioEvaluacion'

export const metadata = {
  title: 'BM Calistenia — Formulario de Evaluación 360°',
}

export default function EvaluacionPage() {
  return <FormularioEvaluacion />
}
